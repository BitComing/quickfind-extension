# 统一换行符与缩进，避免无意义的整文件 diff。
# 项目源码为 2 空格缩进、LF 换行、UTF-8。
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

[*.md]
trim_trailing_whitespace = false
